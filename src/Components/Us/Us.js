import React from "react";
import Sidebar from "../Sidebar/Sidebar";

const Us = ({items}) => {
    return (
        <div>
            <Sidebar items={items}/>
        </div>
    );
};

export default Us;